package app.models;

// El enum TypeClient define los tipos de clientes que pueden existir en el sistema, que son "COMPANY" para clientes empresariales y "NATURAL" para clientes naturales.
public enum TypeClient {
    COMPANY,
    NATURAL
}
